# ClaudeCode Project Backups - Direnv Integration
# Copy this file to your project root as .envrc
# Version: 1.2.0
#
# Usage:
#   1. Copy this file: cp /path/to/integrations/direnv/.envrc /your/project/.envrc
#   2. Allow direnv: direnv allow
#   3. Direnv will auto-load these settings when you cd into this directory

# ==============================================================================
# BACKUP SYSTEM CONFIGURATION
# ==============================================================================

# Path to ClaudeCode Backup System (REQUIRED - update this path)
export CLAUDECODE_BACKUP_ROOT="/Volumes/WORK DRIVE - 4TB/WEB DEV/CLAUDE CODE PROJECT BACKUP"

# Add backup commands to PATH
export PATH="$CLAUDECODE_BACKUP_ROOT/bin:$PATH"

# ==============================================================================
# SHELL INTEGRATION (OPTIONAL)
# ==============================================================================

# Load shell integration (prompt, auto-trigger, aliases)
# Uncomment to enable:
# source "$CLAUDECODE_BACKUP_ROOT/integrations/shell/backup-shell-integration.sh"

# ==============================================================================
# PROJECT-SPECIFIC SETTINGS
# ==============================================================================

# Auto-trigger settings
export BACKUP_AUTO_TRIGGER=true                # Auto-backup on cd
export BACKUP_TRIGGER_INTERVAL=300             # Debounce (seconds)

# Prompt settings
export BACKUP_SHOW_PROMPT=true                 # Show status in prompt
export BACKUP_PROMPT_FORMAT=emoji              # emoji|compact|verbose

# Quick aliases
export BACKUP_ALIASES_ENABLED=true             # Enable bs, bn, etc.

# ==============================================================================
# GIT HOOKS SETTINGS
# ==============================================================================

# Disable specific git hooks for this project
# export BACKUP_GIT_PRE_COMMIT_DISABLED=false
# export BACKUP_GIT_POST_COMMIT_DISABLED=false
# export BACKUP_GIT_PRE_PUSH_DISABLED=false

# Quiet mode (suppress git hook messages)
# export BACKUP_GIT_QUIET=false

# Failure handling
# export BACKUP_GIT_BLOCK_ON_FAILURE=false
# export BACKUP_GIT_BLOCK_PUSH_ON_FAILURE=false

# Pre-push timing
# export BACKUP_GIT_MAX_BACKUP_AGE=3600        # Seconds

# ==============================================================================
# PROJECT METADATA (OPTIONAL)
# ==============================================================================

# These will be picked up by the backup system if set
# export BACKUP_PROJECT_NAME="$(basename "$PWD")"
# export BACKUP_PROJECT_DESCRIPTION="Description of this project"
# export BACKUP_PROJECT_TAGS="tag1,tag2,tag3"

# ==============================================================================
# STARTUP MESSAGE
# ==============================================================================

# Show that direnv loaded successfully (optional)
echo "✅ ClaudeCode Backup System loaded for $(basename "$PWD")"
echo "   Commands: backup status | backup now | backup help"
echo "   Quick: bs | bn | bc | bcl | br"
